React components must be purely presentational.

Components (.tsx) should only render UI and receive data via props.

Business logic, API calls, and data processing must not exist inside UI components.

Components must remain small, reusable, and focused only on layout and rendering.
